let num1 = Number(prompt('Digite o primeiro número: ')) // é necessário converter a variaveis
let num2 = Number(prompt('Digite o segundo número: ')) // pois as variaveis viram tipo string

const resultado = num1 + num2 

alert(`O resultado da soma é ${resultado}`) // posso colocar direto a conta onde será exibido o resultado
